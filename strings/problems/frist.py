# wap in python to enter your full name print the first and the last character the first name last name and the length of the name and the indivisual words 

name = input("enter your full name: ")
len_full = len(str)
arr = str.split(" ")

for i in str :
    arr.append(i)
    
print (f"the detaisl required are :\n{name} : length {len_full}\n{arr[0]} : length {len(arr[0])}\n{arr[1]} : length {len(arr[1])}")